const Success = () => {
  return (
    <section className="flex items-center justify-center h-screen">
      <h3 className="text-3xl font-mono font-semibold">SOPE DONE</h3>
    </section>
  );
};

export default Success;
